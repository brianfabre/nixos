!!!!! in linux mint, go to keyboards > layouts > options
!!!!! and select caps to hyper setting

!!!!! add the following to .profile for autostart
!!!!! if [ -f "$HOME/.Xmodmap" ]; then
!!!!!     "xmodmap $HOME/.Xmodmap"
!!!!! fi


! Unmap capslock
!clear Lock
!keycode 66 = Hyper_L

! Leave mod4 as windows key _only_
remove mod4 = Hyper_L

! Set mod3 to capslock
add mod3 = Hyper_L
